# sven-lutz.github.io

Personal website and notes for Sven Lutz, built with Jekyll and deployed through GitHub Pages.

## Local preview

```bash
bundle install
bundle exec jekyll serve
```

Then open `http://localhost:4000`.

## Publishing

Pushes to `main` are deployed through the GitHub Pages workflow in `.github/workflows/jekyll-gh-pages.yml`.
